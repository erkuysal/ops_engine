# .ops/.tmp/

Ephemeral env files generated by the ops environment broker for stacks that require
a materialized `.env` file (materialization mode: `generated_file`).

Files are created at runtime and cleaned up after service execution.
Do not commit this directory.

Add to `.gitignore`:
```
.ops/.tmp/
```
